@inject HttpClient _http;
@inject ILogger<ListHabits> _log;
@attribute [Authorize]

@if (ShowHeader)
{
    <h3>My Habits</h3>
    <p>Track the completion of your habits on a daily basis.</p>
}

@if (_habits is null)
{
    <LoadingSpinner Label="Loading habits..." />
    <p>This app runs on a free hosting service so this first request may be slow while services restart.</p>
}
else if (_habits.Length <= 0)
{
    <NoHabitsDisplay />
}
else
{
    <div class="col-12 d-flex justify-content-center">
        <table class="table table-sm table-bordered table-striped">
            <thead>
                <tr>
                    <th class="col">Name</th>
                    <th class="col text-center">Status</th>
                    <th class="col text-center"></th>
                    <th class="col text-center"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var habit in _habits)
                {
                    <tr>
                        <td>
                            @habit.Name<a href=@Constants.Habits.DetailsUrl(habit.Id)>
                            <i class="bi bi-link-45deg mx-2"></i></a>
                        </td>
                        <td class="text-center">@habit.ToCompletionString(Date)</td>
                        <td class="text-center mx-0 px-1" style="min-width: 80px;">
                            <LoadingButton IconClass="bi bi-clipboard2-check"  title="Mark habit completion"
                                           style="width: 32px;" OnClick=@(() => OnMarkClicked(habit.Id)) />
                            <LoadingButton IconClass="bi bi-clipboard2-minus" title="Unmark habit completion"
                                           style="width: 32px;" OnClick=@(() => OnUnmarkClicked(habit.Id)) />
                        </td>
                        <td class="text-center">
                            <CompletionMark Habit=habit Date=Date ShowCompletionTitle=false />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

<DisplayErrors Errors=@_errors />
