@inject HttpClient _http;
@inject ILogger<ListHabits> _log;

<h4>Monthly Completions</h4>
@if (_habit.IsSome && _habit.Get() is HabitResponse habit)
{
    <MonthCompletionGrid Habit=habit style="width: 200px; height: 180px; gap: 4px;" />
}

<DisplayErrors Errors=@_errors />
